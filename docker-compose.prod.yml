services:
  sysapps-center-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    ports:
      - "5173:80"
    networks:
      - proxy-net

networks:
  proxy-net:
    external: true
